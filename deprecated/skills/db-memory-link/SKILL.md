# DB Memory Link

## Description

This skill is used to link two memories together in the database.

## Usage

```bash
deno run --allow-net --allow-read --allow-env skills/db-memory-link/script.ts <source_id> <target_id> <relationship_type>
```
